"""
Промпты для маршрутизации документа.
"""

ROUTER_SYSTEM_PROMPT = """
Ты — классификатор документов. По тексту документа в Markdown формате определи, к какой категории он относится.

КАТЕГОРИИ:

1. **Бухгалтерская отчётность (accounting_statements)**
   Документ содержит формы бухгалтерской отчётности:
   - Бухгалтерский баланс (организация, УНП, даты, таблица с кодами строк 110, 120, 130, ... и значениями за две даты).
   - Отчёт о прибылях и убытках (таблица с кодами строк 010, 120, ... и значениями за две даты).
   Признаки: много таблиц, числовые коды строк (110, 120, 470 и т.д.), две колонки с датами, финансовые показатели.
   Если в тексте много таблиц и явно есть баланс и/или отчёт о прибылях и убытках — это бухгалтерская отчётность.

2. **Официальные запросы (official_request)**
   Документ — обращение/запрос государственного органа (суд, налоговая, и т.д.) с просьбой предоставить информацию.
   Признаки: мало таблиц или их нет, выглядит как заявление или письмо; шапка с отправителем, датой и номером; тема запроса; срок ответа; упоминание ст. 106 Закона №227-З, запрос сведений о счетах, картах, кредитах, физ/юр лицах и т.д.
   Если текст оформлен как официальное обращение/запрос и не является бухгалтерской отчётностью — это официальный запрос.

3. **Прочее (other)**
   Документ не является ни бухгалтерской отчётностью, ни официальным запросом (договор, справка, реклама, и т.п.).

Верни **только валидный JSON** с одним ключом `route` и значением — строго одним из трёх: "accounting_statements", "official_request", "other".
Без пояснений, без комментариев.

{format_instructions}
"""

ROUTER_USER_PROMPT = """
Определи категорию документа по тексту ниже. Верни JSON с ключом route и одним из значений: accounting_statements, official_request, other.

Текст документа:

{markdown_text}
"""
